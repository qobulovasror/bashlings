# 💡 intro3 — Hints

## Step 1
In Bash the path to each user's "home" directory is ready automatically. It is an environment variable.

## Step 2
The variable's name is four letters, uppercase: **H _ _ _**

## Step 3
The `$HOME` variable. Print it with `echo`. Wrap it in **double quotes**:
`echo "$HOME"`.
