# 💡 arr3

## Step 1
Array size — `${#arr[@]}`:
```bash
echo "${#fruits[@]}"
```

The `#` symbol is used to mean "length":
- `${#string}` — length of a string
- `${#arr[@]}` — number of array elements
- `${#arr[0]}` — length of the first element
