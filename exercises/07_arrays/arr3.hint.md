# 💡 arr3

## 1-bosqich
Massiv hajmi — `${#arr[@]}`:
```bash
echo "${#fruits[@]}"
```

`#` belgisi "uzunlik" ma'nosida ishlatiladi:
- `${#string}` — string uzunligi
- `${#arr[@]}` — massiv element soni
- `${#arr[0]}` — birinchi element uzunligi

## ✅ Yechim
```bash
echo "${#fruits[@]}"
```
