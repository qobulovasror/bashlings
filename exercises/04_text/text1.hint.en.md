# 💡 text1 — Hints

## Step 1
The `head` command — prints the **beginning** of a file (its first lines).
By default it prints 10 lines.

## Step 2
To control the number of lines, use the `-n` flag:
```bash
head -n 5 fayl.txt   # first 5 lines
```

Or in short form:
```bash
head -5 fayl.txt
```
