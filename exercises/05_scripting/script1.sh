#!/usr/bin/env bash
#
# MASHQ: O'zgaruvchilar va interpolyatsiya
# DARAJA: ★★☆☆☆
# MAVZU: part1/05-basic-scripting · variables
#
# `name` o'zgaruvchisida "Bash" so'zini saqlang.
# So'ngra quyidagi qatorni chiqaring:
#     Salom, Bash!
#
# Maslahat:
#   - O'zgaruvchini probelsiz e'lon qiling: `name="Bash"` (`name = "Bash"` XATO!)
#   - "..." qo'shtirnoq ichida o'zgaruvchi avtomatik almashtiriladi: `"Salom, $name!"`
#   - '...' yagona tirnoq ichida interpolyatsiya bo'lmaydi (xato yo'l)
#
# --- English ---
# TASK: Variables and interpolation
# LEVEL: ★★☆☆☆
# TOPIC: part1/05-basic-scripting · variables
#
# Store the word "Bash" in the `name` variable.
# Then print the following line:
#     Salom, Bash!
#
# Hint:
#   - Declare the variable with no spaces: `name="Bash"` (`name = "Bash"` is WRONG!)
#   - Inside "..." double quotes a variable is substituted automatically: `"Salom, $name!"`
#   - Inside '...' single quotes there is no interpolation (wrong way)

# I AM NOT DONE

# TODO: name o'zgaruvchisiga "Bash" qiymatini bering

# TODO: "Salom, $name!" matnini chiqaring

# === TEST META ===
# @test:stdout: Salom, Bash!
# @test:exit: 0
