# 15 — Docker asoslari

📘 **Kitob bobi:** [`docs/part3/05-docker.md`](../../docs/part3/05-docker.md)

## Mashqlar ro'yxati

| # | Nomi      | Mavzu                                          | Daraja      |
|---|-----------|------------------------------------------------|-------------|
| 1 | `docker1` | `docker run -d -p` — port mapping              | ★☆☆☆☆       |
| 2 | `docker2` | Oddiy Dockerfile (FROM/WORKDIR/COPY/CMD)       | ★★☆☆☆       |
| 3 | `docker3` | `docker run` + volume + env + workdir          | ★★☆☆☆       |
| 4 | `docker4` | `docker ps` output — ID'larni ajratish         | ★★★☆☆       |
| 5 | `docker5` | Image to'liq nomi (registry/owner/name:tag)    | ★★★☆☆       |
| 6 | `docker6` | Multi-stage Dockerfile (FROM ... AS, --from)   | ★★★☆☆       |
| 7 | `docker7` | docker-compose.yml minimal                     | ★★★★☆       |

> Bu mashqlar **Docker daemon talab qilmaydi** — sintaksis va konfiguratsiya.
> Real konteynerlar bilan ishlash uchun kitob bobining oxiridagi misollarni
> ko'ring.

## Boshlash

```bash
bashlings watch
```
