# 💡 ssh5

## 1-bosqich
rsync sintaksisi:
```
rsync <FLAGS> <SOURCE> <USER>@<HOST>:<DEST>
```

## 2-bosqich
Eng klassik flag kombinatsiyasi: `-avz --delete`

## 3-bosqich
`/` farqi muhim:
- `./dist`   — papkani nusxalash
- `./dist/`  — ichidagi fayllarni nusxalash

Bu mashqda `/dist/` ishlatamiz (ichidagi).
