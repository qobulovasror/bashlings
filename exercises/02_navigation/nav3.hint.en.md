# 💡 nav3 — Hints

## Step 1
`mkdir` accepts several arguments at once. Example: `mkdir a b c` — creates 3 directories.

## Step 2
The `-p` flag — also creates any missing parent directories in the path. And it does not error if the directory already exists.

## Step 3
Your task: `docs`, `src`, `tests` — 3 directories. `ls` lists them in alphabetical order.
