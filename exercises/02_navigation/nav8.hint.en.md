# 💡 nav8 — Hints

## Step 1
`rm <fayl>` — deletes. **Be careful** — `rm` is an irreversible delete. It does not go to the `Trash`.

## Step 2
You need to delete one file and see the rest. `ls` with no arguments lists the current directory.

## Step 3
**Attention:** `rm tmp*.txt` would delete all 3 files. We only need **tmp2.txt** — do not use a wildcard, delete it by its exact name.
