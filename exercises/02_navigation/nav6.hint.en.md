# 💡 nav6 — Hints

## Step 1
`cp` takes two arguments: **source** and **destination**.
```bash
cp manba.txt nusxa.txt
```

## Step 2
To print a file's contents use `cat <fayl>`.

## Step 3
You can write the two commands on separate lines, or chain them with `&&`.
