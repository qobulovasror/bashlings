# 💡 nav7 — Hints

## Step 1
`mv` does two jobs in one command:
- **Move**: `mv fayl.txt /tmp/` — to another directory
- **Rename**: `mv eski.txt yangi.txt` — change the name

## Step 2
`mv` does not require the `-r` flag for a directory.
